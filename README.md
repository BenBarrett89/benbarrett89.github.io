# BenBarrett89

Personal site and small web applications, built with Next.js and Tailwind CSS.

## Development

```sh
npm install
npm run dev
```

## Production build

```sh
npm run build
```

The build uses Next.js static export mode and writes deployable files to `out/` for GitHub Pages.
